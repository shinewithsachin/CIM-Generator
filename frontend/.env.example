# Base URL for the backend API. Leave unset in local dev — Vite's dev-server
# proxy (see vite.config.js) forwards /api to http://localhost:8000.
# Set explicitly for a production build served from a different origin, e.g.:
# VITE_API_URL=https://api.your-domain.com/api
VITE_API_URL=
